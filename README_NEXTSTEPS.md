Next steps checklist:

- Add `public/avatar.png` or stylized avatar.
- Replace placeholder project data in `src/components/Projects.tsx`.
- Add `resume.pdf` to `public/` and update download link.
- Implement command palette and keyboard shortcuts.
- Add tests and CI (GitHub Actions).
- Deploy to Vercel.
